# ================================================
# Garage Manager - Production Configuration
# نظام إدارة الورشة - إعدادات الإنتاج
# ================================================
# قم بنسخ هذا الملف إلى .env وتعديله حسب احتياجاتك
# cp env.example.txt .env

# ========== Flask Configuration ==========
FLASK_APP=app.py
FLASK_ENV=production
SECRET_KEY=change-this-to-a-strong-random-secret-key-in-production

# ========== Database Configuration ==========
# SQLite (الافتراضي - مناسب للتطبيقات الصغيرة والمتوسطة)
DATABASE_URL=sqlite:///instance/app.db

# PostgreSQL (للتطبيقات الكبيرة - اختياري)
# DATABASE_URL=postgresql://db_user:db_pass@localhost:5432/garage_db

# ========== Server Configuration ==========
HOST=0.0.0.0
PORT=5000
DEBUG=false
MAX_CONTENT_LENGTH=52428800

# ========== Security ==========
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax
PERMANENT_SESSION_LIFETIME=86400

# ========== Uploads & Storage ==========
UPLOAD_FOLDER=static/uploads
MAX_UPLOAD_SIZE=10485760
ALLOWED_EXTENSIONS=png,jpg,jpeg,gif,pdf,xlsx,xls,csv

# ========== Email Configuration (اختياري) ==========
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=
MAIL_PASSWORD=

# ========== Redis Configuration (اختياري - للكاش) ==========
REDIS_URL=redis://localhost:6379/0
REDIS_ENABLED=false

# ========== Rate Limiting ==========
RATELIMIT_ENABLED=true
RATELIMIT_DEFAULT=200 per day;50 per hour

# ========== Monitoring (اختياري) ==========
SENTRY_DSN=
PROMETHEUS_ENABLED=false

# ========== AI Assistant (اختياري) ==========
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ========== Backup Configuration ==========
BACKUP_ENABLED=true
BACKUP_INTERVAL=daily
BACKUP_RETENTION_DAYS=30
BACKUP_PATH=instance/backups

# ========== Logging ==========
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=5

# ========== Currency & Exchange ==========
DEFAULT_CURRENCY=ILS
AUTO_UPDATE_RATES=true
EXCHANGE_RATE_API_KEY=

# ========== Application Settings ==========
APP_NAME=Garage Manager
APP_VERSION=1.0.0
TIMEZONE=Asia/Jerusalem
LANGUAGE=ar

